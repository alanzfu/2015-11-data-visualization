//data is currently stored as [{}....{}]
data = data['data'];
var datum = data[data.length-1]
datum.cause = property that is true// this is the oldest data entry
  //loop through 14-31
datum.neighborhood

var causeString = datum[whichever one is true];

updateCities = [];
